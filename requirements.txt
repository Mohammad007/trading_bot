# Core async + HTTP
aiohttp>=3.9
aiofiles>=23.0
websockets>=12.0
requests>=2.32
httpx>=0.27
tenacity>=8.3

# Data + ML (CPU-friendly). Loose pins so they resolve on 3.11 / 3.12 / 3.13.
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
xgboost>=2.1
joblib>=1.4

# Solana
solders>=0.21
solana>=0.34
base58>=2.1
construct>=2.10

# EVM (Ethereum/BSC/Polygon/Base/Arbitrum/Optimism/Avalanche)
web3>=6.20
eth-account>=0.13
eth-abi>=5.1
hexbytes>=1.2

# Tron
tronpy>=0.5

# Terminal UI
rich>=13.7
colorama>=0.4

# Telegram
python-telegram-bot>=21.3

# Config / secrets
python-dotenv>=1.0
cryptography>=42.0

# Utilities
pydantic>=2.7
orjson>=3.10

# NOTE: TensorFlow is OPTIONAL. The LSTM module has a statistical fallback,
# so the bot runs fine without it. If you want the real LSTM model, see
# requirements-ml.txt - it requires Python 3.10-3.12 (no 3.13 wheels yet).
#
# NOTE on Sui/Aptos: Python SDKs for these chains are not production-stable.
# They are intentionally NOT included. If you need them, add by hand:
#   pip install pysui aptos-sdk
# and expect to debug.
